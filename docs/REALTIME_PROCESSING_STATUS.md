# Real-Time Processing Status — v1.5.7

The Processing Bottle Match dialog now includes a scrolling live status frame.

It shows:

- frontend processing stages as they happen
- backend processing trace after the comparison returns
- PASS / FAIL / INFO labels
- final decision status

The purpose is to let users see what the system is doing during comparison instead of waiting silently.
